<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Color Brushes for different categories -->
    <!-- Color Resources -->
    <SolidColorBrush x:Key="WorkBrush"
                     Color="#3B82F6" />
    <SolidColorBrush x:Key="WorkLightBrush"
                     Color="#EFF6FF" />
    <SolidColorBrush x:Key="WorkBorderBrush"
                     Color="#DBEAFE" />

    <!-- Progress Bar Style -->
    <Style x:Key="CategoryProgressBar"
           TargetType="ProgressBar">
        <Setter Property="Height"
                Value="8" />
        <Setter Property="Foreground"
                Value="{Binding CategoryBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Setter Property="Background"
                Value="{Binding ProgressBackgroundBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border CornerRadius="4"
                            Background="{TemplateBinding Background}"
                            ClipToBounds="True">
                        <Rectangle Name="PART_Indicator"
                                   Fill="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Left" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>