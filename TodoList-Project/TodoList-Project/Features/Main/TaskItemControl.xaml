<UserControl x:Class="TodoList_Project.Features.Main.TaskItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TodoList_Project.Features.Main"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="400"
             Background="Transparent">
    <Border BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="8"
            Margin="5"
            Padding="15"
            Background="{Binding Background}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="{Binding Title}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#212121"
                       Margin="0,0,0,8" />

            <!-- Due Date -->
            <TextBlock Grid.Row="1"
                       Text="{Binding DueDate, StringFormat='Due: {0}'}"
                       FontSize="14"
                       Foreground="#424242"
                       Margin="0,0,0,4" />

            <!-- Created Date -->
            <TextBlock Grid.Row="2"
                       Text="{Binding DaysAgo}"
                       FontSize="12"
                       Foreground="#757575"
                       Margin="0,0,0,8" />

            <!-- Priority -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Ellipse Width="12"
                         Height="12"
                         Style="{StaticResource PriorityEllipseStyle}"
                         Margin="0,0,8,0" />
                <TextBlock Text="{Binding Priority}"
                           FontSize="14"
                           FontWeight="Medium"
                           Foreground="#424242" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>