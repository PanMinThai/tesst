<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Priority to Color Converter -->
    <Style x:Key="PriorityEllipseStyle" TargetType="Ellipse">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Priority}" Value="High">
                <Setter Property="Fill" Value="#FF5722"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Priority}" Value="Medium">
                <Setter Property="Fill" Value="#FF9800"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Priority}" Value="Low">
                <Setter Property="Fill" Value="#4CAF50"/>
            </DataTrigger>
        </Style.Triggers>
        <Setter Property="Fill" Value="#757575"/>
    </Style>

    <!-- Status to Color Converter -->
    <Style x:Key="StatusBorderStyle" TargetType="Border">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Status}" Value="InProgress">
                <Setter Property="Background" Value="#FF9800"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="Completed">
                <Setter Property="Background" Value="#4CAF50"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                <Setter Property="Background" Value="#F44336"/>
            </DataTrigger>
        </Style.Triggers>
        <Setter Property="Background" Value="#757575"/>
    </Style>
</ResourceDictionary>